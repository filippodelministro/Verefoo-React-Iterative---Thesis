% !TEX root = ../tesi.tex

\section{Idea of the Solution}
% - Invece di processare tutti gli NSR insieme, introdurli uno alla volta
% - Ad ogni iterazione: configurazione corrente = stato iniziale, React-VEREFOO aggiunge un NSR
% - Schema: iter 1 => initProp + A, iter 2 => (initProp+A) + B, iter 3 => (initProp+A+B) + C, ...
% - Ogni sottoproblema è più piccolo => potenziale riduzione del tempo di solving
% - Trade-off atteso: tempo totale ridotto vs possibile perdita di ottimalità globale

\section{Implementation}
% - File modificati in React-VEREFOO e come
% - Gestione delle properties tra iterazioni
% - Struttura del loop iterativo
% - Come viene passato lo stato (configurazione) da un'iterazione alla successiva
% TODO: completare con i dettagli specifici dell'implementazione

\section{Example Run}
% - Mostrare un esempio concreto di esecuzione
% - Input: topologia e insieme di NSR di esempio
% - Output iterazione per iterazione: quali regole vengono aggiunte
% - Output finale: configurazione completa
% TODO: inserire screenshot o listing dell'output
